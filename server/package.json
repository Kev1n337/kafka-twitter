{
  "name": "kafka-server",
  "version": "1.0.0",
  "description": "A rest server piping kafka streams",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kevin Linne",
  "license": "ISC",
  "dependencies": {
    "kafka-node": "^2.6.1"
  }
}
